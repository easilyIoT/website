import React from "react"

import { Container, Row, Col } from "react-bootstrap"

const CenterInScreen = ({ children }) => (
        <div className="text-center h-100">{children}</div>
);


export default CenterInScreen;